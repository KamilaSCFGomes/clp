#   struct -> estrutura imutável
#   mutable struct -> estrutura mutável
struct Entidade
    id::int64
    #   tipagem paramétrica flexível: tipagem estática é opcional
    #   e tipos podem receber parâmetros
end

